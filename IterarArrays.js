var frutas = ["melancia", "acerola", "laranja", "banana"];
console.log(frutas)

frutas.map((fruta, index) => `${index} - ${fruta} `)

// flat

/*  Retorna todos os elementos de um array com todos os elementos de um sub-array
 concatenados de forma recursiva de acordo com a profundidade especificada (depth)


*/